# SimpleImageCaptchaValidator 实现计划

## [ ] 任务1：分析Java版与.NET版的差异
- **优先级**：P0
- **依赖**：无
- **描述**：
  - 分析SimpleImageCaptchaValidator类的当前实现
  - 识别与Java版的具体差异点
  - 确定需要修改的部分
- **成功标准**：
  - 明确识别出所有与Java版不一致的地方
  - 理解Java版的预期行为
- **测试要求**：
  - `programmatic` TR-1.1：运行现有测试，确认当前行为
  - `human-judgement` TR-1.2：代码审查，确认差异点
- **备注**：重点关注点击验证码的坐标计算和容错值计算逻辑

## [ ] 任务2：修复点击验证码的坐标处理
- **优先级**：P0
- **依赖**：任务1
- **描述**：
  - 确保点击验证码的坐标处理与Java版一致
  - 检查并修复坐标计算逻辑
  - 确保中心坐标的计算方式正确
- **成功标准**：
  - 点击验证码的坐标处理与Java版一致
  - 测试通过
- **测试要求**：
  - `programmatic` TR-2.1：运行点击验证码相关测试
  - `human-judgement` TR-2.2：代码审查，确认坐标处理逻辑正确
- **备注**：参考代码中的注释，Java版存储的是左上角坐标，而.NET版需要计算中心坐标

## [ ] 任务3：修复容错值计算
- **优先级**：P0
- **依赖**：任务1
- **描述**：
  - 确保容错值的计算与Java版一致
  - 检查并修复容错值的计算逻辑
  - 确保基于字符宽度的动态容错值计算正确
- **成功标准**：
  - 容错值的计算与Java版一致
  - 测试通过
- **测试要求**：
  - `programmatic` TR-3.1：运行容错值相关测试
  - `human-judgement` TR-3.2：代码审查，确认容错值计算逻辑正确
- **备注**：参考代码中的注释，Java版基于第一个字符的宽度计算容错值

## [ ] 任务4：修复验证码类型分类
- **优先级**：P1
- **依赖**：任务1
- **描述**：
  - 检查并修复CaptchaTypeClassifier类中的字符串类型判断
  - 确保类型分类逻辑与Java版一致
- **成功标准**：
  - 验证码类型分类逻辑与Java版一致
  - 测试通过
- **测试要求**：
  - `programmatic` TR-4.1：运行类型分类相关测试
  - `human-judgement` TR-4.2：代码审查，确认类型分类逻辑正确
- **备注**：参考CaptchaTypeClassifier类中的TODO注释

## [ ] 任务5：运行完整测试套件
- **优先级**：P1
- **依赖**：任务2、任务3、任务4
- **描述**：
  - 运行完整的测试套件
  - 确保所有测试通过
  - 验证修复是否成功
- **成功标准**：
  - 所有测试通过
  - 实现与Java版一致
- **测试要求**：
  - `programmatic` TR-5.1：运行所有测试
  - `human-judgement` TR-5.2：代码审查，确认修复完整
- **备注**：确保修复后的实现与Java版完全一致